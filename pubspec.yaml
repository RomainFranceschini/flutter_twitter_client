name: twitter_client
description: A new Flutter project.

publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  twitter_api_v2: ^4.4.0
  twitter_oauth2_pkce: ^1.0.0
  go_router: ^5.1.10

  cupertino_icons: ^1.0.2
  firebase_auth: ^4.1.3
  firebase_core: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/feed.json
